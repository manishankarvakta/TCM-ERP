# Docker volumes - contains persistent data
volumes/

# Explicit local volume roots (absolute host bind-mount targets)
volumes/postgres/
volumes/minio/
volumes/redis/

# Environment files
.env.docker
.env.docker.local

# Keep the directory structure
!volumes/.gitkeep


.DS_Store
