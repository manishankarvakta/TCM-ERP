# Production Docker Compose with Absolute Paths
# Data persists FOREVER at fixed locations, regardless of code changes

services:
  ts-crm-postgres:
    image: postgres:16-alpine
    container_name: ts-crm-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-ts-crm}
    volumes:
      # ABSOLUTE PATH - Data stays here forever
      # Even if you delete entire project directory
      - /var/lib/ts-crm/postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - docker-network

  ts-crm-minio:
    image: minio/minio
    container_name: ts-crm-minio
    restart: unless-stopped
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:?MINIO_ROOT_USER is required}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:?MINIO_ROOT_PASSWORD is required}
      MINIO_API_CORS_ALLOW_ORIGIN: ${MINIO_CORS_ORIGIN:-*}
    expose:
      - "9000"
      - "9001"
   
    volumes:
      # ABSOLUTE PATH - All uploaded files stay here forever
      - /var/lib/ts-crm/minio:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 20s
      retries: 3
    networks:
      - docker-network
      - dokploy-network

  ts-crm-minio-setup:
    image: minio/mc:latest
    container_name: ts-crm-minio-setup
    depends_on:
      ts-crm-minio:
        condition: service_healthy
    entrypoint: >
      /bin/sh -c "
      sleep 5;
      echo 'Setting up MinIO bucket...';
      /usr/bin/mc alias set myminio http://ts-crm-minio:9000 ${MINIO_ROOT_USER:-minioadmin} ${MINIO_ROOT_PASSWORD:-minioadmin};
      /usr/bin/mc mb myminio/${MINIO_BUCKET_NAME:-ts-crm-files} || echo 'Bucket already exists';
      /usr/bin/mc anonymous set download myminio/${MINIO_BUCKET_NAME:-ts-crm-files} || true;
      /usr/bin/mc anonymous set upload myminio/${MINIO_BUCKET_NAME:-ts-crm-files} || true;
      /usr/bin/mc anonymous set public myminio/${MINIO_BUCKET_NAME:-ts-crm-files} || true;
      echo 'MinIO bucket setup complete!';
      exit 0;
      "
    networks:
      - docker-network

  ts-crm-app:
    build:
      context: ./ts-crm
      dockerfile: Dockerfile
    container_name: ts-crm-app
    restart: unless-stopped
    expose:
      - "3000"
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}@ts-crm-postgres:5432/${POSTGRES_DB:-startup_mvp}?schema=public
      NEXTAUTH_SECRET: ${NEXTAUTH_SECRET:?NEXTAUTH_SECRET is required}
      NEXTAUTH_URL: ${NEXTAUTH_URL:?NEXTAUTH_URL is required}
      AUTH_TRUST_HOST: ${AUTH_TRUST_HOST:-true}
      # MINIO_ENDPOINT: ts-crm-minio
      MINIO_PORT: 9000
      MINIO_USE_SSL: ${MINIO_USE_SSL:-false}
      MINIO_ACCESS_KEY: ${MINIO_ROOT_USER:?MINIO_ROOT_USER is required}
      MINIO_SECRET_KEY: ${MINIO_ROOT_PASSWORD:?MINIO_ROOT_PASSWORD is required}
      MINIO_BUCKET_NAME: ${MINIO_BUCKET_NAME:-ts-crm-files}
      MINIO_PUBLIC_URL: ${MINIO_PUBLIC_URL:-http://ts-crm-minio:9000}
      MINIO_ENDPOINT: ${MINIO_ENDPOINT:-ts-crm-minio}
      REDIS_URL: ${REDIS_URL:-redis://ts-crm-redis:6379}
      SMTP_HOST: ${SMTP_HOST:-}
      SMTP_PORT: ${SMTP_PORT:-465}
      SMTP_SECURE: ${SMTP_SECURE:-true}
      SMTP_USER: ${SMTP_USER:-}
      SMTP_PASS: ${SMTP_PASS:-}
      EMAIL_FROM: ${EMAIL_FROM:-}
      EMAIL_FROM_NAME: ${EMAIL_FROM_NAME:-Startup MVP}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL:?NEXT_PUBLIC_APP_URL is required}
      NODE_ENV: production
    depends_on:
      ts-crm-postgres:
        condition: service_healthy
      ts-crm-minio:
        condition: service_healthy
    networks:
      - docker-network
      - dokploy-network

  ts-crm-redis:
    image: redis:7-alpine
    container_name: ts-crm-redis
    restart: unless-stopped
    volumes:
      # ABSOLUTE PATH - Cache data stays here
      - /var/lib/ts-crm/redis:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - docker-network

networks:
  docker-network:
    driver: bridge
  dokploy-network:
    external: true